bottle==0.13.4
waitress==3.0.2
selenium==4.47.0
func-timeout==4.3.5
prometheus-client==0.26.0
# Required by undetected_chromedriver
requests==2.34.2
certifi==2026.7.22
websockets==17.0.1
packaging==26.3
# Only required for Linux and macOS
xvfbwrapper==0.2.29; platform_system != "Windows"
# Only required for Windows
pefile==2024.8.26; platform_system == "Windows"
# Stealth engine (Camoufox + playwright-captcha), mirrors Byparr's stack
playwright==1.60.*
playwright-captcha==0.1.*
# Pinned exactly: this package carries the patched Firefox (it pins
# invisible-core in turn), so a bad bump breaks solving while the build stays
# green. Published releases only, never the git head.
invisible-playwright==0.7.2
